# Normalize line endings to LF in the repo on every platform (Windows included).
* text=auto eol=lf

# Binary files must never be normalized.
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.lock binary