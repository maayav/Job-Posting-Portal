# Frontend environment
# The Vite dev server proxies /api to http://localhost:5000 (see vite.config.js).
# No env vars are required for local development. For production, set:
# VITE_API_BASE=https://your-api.example.com